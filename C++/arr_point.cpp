#include<iostream>
int main()
{
    // int a[]={1,2,3,4,5,6,7,8,9};
    int a[2][9];
    // int a[2][9]={{111,22,3,4,5,6,7,8,9},{11,22,33,44,55,66,77,88,99}};
    int i,j,k=0;
    int *res=&a[0][0];
    // for(int i=0;i<10;i++)
    //     std::cout<<*(*a+i)<<' '<<*(*(a+1)+i)<<'\n';
    for(i=0;i<2;i++)
        for(j=0;j<9;j++)
            std::cout<<&a[i][j]<<' '<<(*(a+i)+j)<<' '<<(res+k++)<<'\n';
    // std::cout<<*(*a+1)<<' '<<*(*a+2)<<'\n';
    // std::cout<<(*a+1)<<' '<<(*a+2)<<'\n';
    // std::cout<<(*(*a+1)<*(*a+2))<<'\n';
    // std::cout<<((*a+1)<(*a+2))<<'\n';

}